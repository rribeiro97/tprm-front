/**
 * Re-export useAuth hook from AuthContext
 *
 * This file provides a cleaner import path for the auth hook
 * while keeping the implementation in the context file.
 */
export { useAuth } from '@/contexts/AuthContext';
